'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
const formatType = require('./formatType.cjs');
function formatCommandCall(name, metadata) {
  let output = name;
  if (metadata.params) {
    metadata.params.forEach(param => {
      output += ' ';
      output += formatType.formatType(param, true);
    });
  }
  return output;
}
exports.formatCommandCall = formatCommandCall;
//# sourceMappingURL=formatCommandCall.cjs.map
