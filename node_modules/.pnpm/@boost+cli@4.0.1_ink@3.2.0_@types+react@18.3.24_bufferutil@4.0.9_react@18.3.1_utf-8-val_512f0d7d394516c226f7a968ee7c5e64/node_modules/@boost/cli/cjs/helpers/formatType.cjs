'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
const applyStyle = require('./applyStyle.cjs');
function formatType(config, inline = false) {
  let type = config.type + (config.multiple ? '[]' : '');
  if (inline) {
    if (config.label) {
      type = config.label;
    }
    type = config.required ? type : `[${type}]`;
  }
  return applyStyle.applyStyle(type, 'muted');
}
exports.formatType = formatType;
//# sourceMappingURL=formatType.cjs.map
