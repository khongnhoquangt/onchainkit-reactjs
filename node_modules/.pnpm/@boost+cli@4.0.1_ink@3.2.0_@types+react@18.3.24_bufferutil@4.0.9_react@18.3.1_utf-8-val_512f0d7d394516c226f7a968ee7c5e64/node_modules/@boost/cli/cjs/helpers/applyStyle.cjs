'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
const terminal = require('@boost/terminal');
const loadTheme = require('./loadTheme.cjs');
function applyStyle(text, type) {
  const color = loadTheme.loadTheme()[type];
  const apply = color.startsWith('#') ? terminal.style.hex(color) : terminal.style[color];
  return apply(text);
}
exports.applyStyle = applyStyle;
//# sourceMappingURL=applyStyle.cjs.map
