'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
const optimal = require('@boost/common/optimal');
const translate = require('../translate.cjs');
const blueprints = require('./blueprints.cjs');
function validateParams(params) {
  params.forEach((config, index) => optimal.optimal(blueprints.paramBlueprint, {
    name: translate.msg('cli:labelParam', {
      name: config.label ?? index
    }),
    unknown: false
  }).validate(config));
}
exports.validateParams = validateParams;
//# sourceMappingURL=validateParams.cjs.map
