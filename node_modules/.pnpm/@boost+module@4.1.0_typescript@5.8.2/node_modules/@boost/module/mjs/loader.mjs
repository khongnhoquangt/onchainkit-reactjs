// Bundled with Packemon: https://packemon.dev
// Platform: node, Support: stable, Format: mjs

import { t as typescript } from './bundle-2f129f90.mjs';
export { t as typescript } from './bundle-2f129f90.mjs';
import 'fs';

// Since we only have 1 loader at the moment, just return it directly.
// Once we have multiple loaders, we can iterate through them as a whole.

const load = typescript.load,
  resolve = typescript.resolve;
export { load, resolve };
//# sourceMappingURL=loader.mjs.map
