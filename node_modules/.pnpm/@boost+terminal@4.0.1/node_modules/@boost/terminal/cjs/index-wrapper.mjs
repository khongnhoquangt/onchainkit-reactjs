// Bundled with Packemon: https://packemon.dev
// This is an MJS wrapper for a sibling CJS file

import data from './index.cjs';

export { figures } from 'figures';
export { style } from 'chalk';
export const { ANSI_REGEX, annotate, calculateWidth, cursor, hasAnsi, link, screen, sliceAnsi, stripAnsi, truncate, wrapAnsi } = data;