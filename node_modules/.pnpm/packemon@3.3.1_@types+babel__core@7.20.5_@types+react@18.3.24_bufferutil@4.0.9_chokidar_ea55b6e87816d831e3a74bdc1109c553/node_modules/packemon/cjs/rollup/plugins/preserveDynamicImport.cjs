'use strict';

const shouldKeepDynamicImport = require('../../helpers/shouldKeepDynamicImport.cjs');
function preserveDynamicImport(platform, support) {
  const preserve = shouldKeepDynamicImport.shouldKeepDynamicImport(platform, support);
  return {
    name: 'packemon-preserve-dynamic-import',
    renderDynamicImport() {
      if (preserve) {
        return {
          left: 'import(',
          right: ')'
        };
      }
      return null;
    }
  };
}
exports.preserveDynamicImport = preserveDynamicImport;
//# sourceMappingURL=preserveDynamicImport.cjs.map
