import { Command, GlobalOptions, PrimitiveType } from '@boost/cli';
import { PackageValidator } from '../PackageValidator';
import { Packemon } from '../Packemon';
export interface CommonOptions {
    cwd: string;
    skipPrivate: boolean;
}
export declare abstract class BaseCommand<O extends object = {}, P extends PrimitiveType[] = string[]> extends Command<CommonOptions & GlobalOptions & O, P> {
    cwd: string;
    debug: boolean;
    formats: string;
    loadConfigs: boolean;
    platforms: string;
    quiet: boolean;
    skipPrivate: boolean;
    protected get packemon(): Packemon;
    protected getPackage(): Promise<import("..").Package>;
    protected renderValidator(validator: PackageValidator): void;
}
//# sourceMappingURL=Base.d.ts.map