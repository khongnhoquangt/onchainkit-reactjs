'use strict';

const cli = require('@boost/cli');
const BuildWorkspace = require('./BuildWorkspace.cjs');
var _dec, _class;
let PackWorkspaceCommand = (_dec = cli.Config('pack-workspace', 'Clean, build, and validate all packages across the workspace'), _dec(_class = class PackWorkspaceCommand extends BuildWorkspace.BuildWorkspaceCommand {
  async run() {
    if (!process.env.NODE_ENV) {
      process.env.NODE_ENV = 'production';
    }
    await this.runPipeline(this.pack.bind(this));
  }
}) || _class);
exports.PackWorkspaceCommand = PackWorkspaceCommand;
//# sourceMappingURL=PackWorkspace.cjs.map
