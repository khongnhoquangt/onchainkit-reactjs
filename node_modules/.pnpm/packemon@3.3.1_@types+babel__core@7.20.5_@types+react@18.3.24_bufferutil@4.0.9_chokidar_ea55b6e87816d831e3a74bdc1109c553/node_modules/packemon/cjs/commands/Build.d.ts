import { Package } from '../Package';
import { BuildOptions } from '../types';
import { BaseCommand } from './Base';
export declare class BuildCommand extends BaseCommand<Required<BuildOptions>> {
    addEngines: boolean;
    addExports: boolean;
    addFiles: boolean;
    concurrency: number;
    declaration: boolean;
    timeout: number;
    stamp: boolean;
    run(): Promise<void>;
    protected build(pkg: Package): Promise<void>;
    protected pack(pkg: Package): Promise<void>;
}
//# sourceMappingURL=Build.d.ts.map