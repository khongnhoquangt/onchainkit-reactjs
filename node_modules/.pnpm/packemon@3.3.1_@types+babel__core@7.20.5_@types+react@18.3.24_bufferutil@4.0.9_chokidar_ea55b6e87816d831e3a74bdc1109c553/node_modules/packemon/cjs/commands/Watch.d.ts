/// <reference types="node" />
import { Package } from '../Package';
import { BaseCommand } from './Base';
export interface WatchOptions {
    debounce: number;
    poll: boolean;
}
export declare class WatchCommand extends BaseCommand<WatchOptions> {
    debounce: number;
    poll: boolean;
    protected package: Package | null;
    protected rebuild: boolean;
    protected rebuilding: boolean;
    protected rebuildTimer?: NodeJS.Timeout;
    enqueueRebuild(event: string, path: string): void;
    run(): Promise<void>;
    triggerRebuild(): void;
    rebuildPackage(): Promise<void>;
}
//# sourceMappingURL=Watch.d.ts.map