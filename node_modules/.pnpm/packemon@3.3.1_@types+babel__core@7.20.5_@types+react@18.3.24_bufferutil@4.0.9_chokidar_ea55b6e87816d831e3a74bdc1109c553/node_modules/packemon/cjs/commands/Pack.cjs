'use strict';

const cli = require('@boost/cli');
const Build = require('./Build.cjs');
var _dec, _class;
let PackCommand = (_dec = cli.Config('pack', 'Clean, build, and validate a package for distribution'), _dec(_class = class PackCommand extends Build.BuildCommand {
  async run() {
    if (!process.env.NODE_ENV) {
      process.env.NODE_ENV = 'production';
    }
    await this.pack(await this.getPackage());
  }
}) || _class);
exports.PackCommand = PackCommand;
//# sourceMappingURL=Pack.cjs.map
