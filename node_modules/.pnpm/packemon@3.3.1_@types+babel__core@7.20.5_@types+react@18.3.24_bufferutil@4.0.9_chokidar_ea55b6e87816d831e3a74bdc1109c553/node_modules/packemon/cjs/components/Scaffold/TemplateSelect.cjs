'use strict';

const react = require('react');
const react$1 = require('@boost/cli/react');
const jsxRuntime = require('react/jsx-runtime');
function TemplateSelect({
  defaultTemplate,
  onSelect
}) {
  const options = react.useMemo(() => [{
    label: 'Monorepo infrastructure (many packages)',
    value: 'monorepo'
  }, {
    label: 'Monorepo package',
    value: 'monorepo-package'
  }, {
    label: 'Polyrepo (single package)',
    value: 'polyrepo-package'
  }], []);
  const validate = react.useCallback(value => {
    if (!value) {
      throw new Error('Please select a template');
    }
  }, []);
  return /*#__PURE__*/jsxRuntime.jsx(react$1.Select, {
    defaultSelected: defaultTemplate,
    label: "Template to scaffold?",
    options: options,
    validate: validate,
    onSubmit: onSelect
  });
}
exports.TemplateSelect = TemplateSelect;
//# sourceMappingURL=TemplateSelect.cjs.map
