'use strict';

const react = require('@boost/cli/react');
const jsxRuntime = require('react/jsx-runtime');
function Symbol({
  depth = [],
  first,
  last
}) {
  return /*#__PURE__*/jsxRuntime.jsxs(react.Style, {
    type: "muted",
    children: [depth.map(d => d ? '│ ' : '  ').join(''), first ? '┌─ ' : last ? '└─ ' : '├─ ']
  });
}
exports.Symbol = Symbol;
//# sourceMappingURL=Symbol.cjs.map
