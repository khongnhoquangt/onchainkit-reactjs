'use strict';

const node_module = require('node:module');
var _documentCurrentScript = typeof document !== 'undefined' ? document.currentScript : null;
const modRequire = node_module.createRequire(typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : _documentCurrentScript && _documentCurrentScript.src || new URL('helpers/loadModule.cjs', document.baseURI).href);
function loadModule(name, message) {
  try {
    return modRequire(name);
  } catch {
    throw new Error(`${message} Please install with \`yarn add --dev ${name}\`.`);
  }
}
exports.loadModule = loadModule;
//# sourceMappingURL=loadModule.cjs.map
