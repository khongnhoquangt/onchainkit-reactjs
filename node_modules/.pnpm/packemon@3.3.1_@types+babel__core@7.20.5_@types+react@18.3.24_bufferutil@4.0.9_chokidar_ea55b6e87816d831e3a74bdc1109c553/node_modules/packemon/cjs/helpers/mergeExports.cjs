'use strict';

/* eslint-disable no-param-reassign */
function mergeExports(prev, base) {
  const next = typeof base === 'string' ? {
    default: base
  } : base;
  if (typeof prev === 'string') {
    return next;
  }
  Object.entries(next).forEach(([origKey, nextValue]) => {
    const key = origKey;
    const prevValue = prev[key];
    if (!prevValue) {
      prev[key] = nextValue;
    } else if (nextValue) {
      prev[key] = mergeExports(prevValue, nextValue);
    }
  });
  return prev;
}
exports.mergeExports = mergeExports;
//# sourceMappingURL=mergeExports.cjs.map
