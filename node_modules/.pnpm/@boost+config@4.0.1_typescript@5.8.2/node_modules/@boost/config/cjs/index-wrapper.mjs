// Bundled with Packemon: https://packemon.dev
// This is an MJS wrapper for a sibling CJS file

import data from './index.cjs';

export const { ConfigError, Configuration, createExtendsSchema, createOverridesSchema, createPluginsSchema, getEnv, mergeArray, mergeExtends, mergeObject, mergePlugins, overwrite } = data;