'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
const common = require('@boost/common');
const mergeArray = require('./mergeArray.cjs');

/**
 * Merges previous and next file paths (either a string or array of strings) into a
 * new list of file paths. This is useful if utilizing config extending.
 */
function mergeExtends(prev, next) {
  return mergeArray.mergeArray(common.toArray(prev), common.toArray(next));
}
exports.mergeExtends = mergeExtends;
//# sourceMappingURL=mergeExtends.cjs.map
