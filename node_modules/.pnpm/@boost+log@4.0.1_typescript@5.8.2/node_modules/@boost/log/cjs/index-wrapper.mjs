// Bundled with Packemon: https://packemon.dev
// This is an MJS wrapper for a sibling CJS file

import data from './index.cjs';

export const { ConsoleTransport, DEFAULT_LABELS, FileTransport, LOG_LEVELS, Logger, MAX_LOG_SIZE, RotatingFileTransport, StreamTransport, Transport, createLogger, formats } = data;