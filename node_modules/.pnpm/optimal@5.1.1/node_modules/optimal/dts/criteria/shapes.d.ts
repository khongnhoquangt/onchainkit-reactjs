import { Blueprint, Criteria, SchemaState } from '../types';
/**
 * Require a shape to be an exact. No more and no less of the same properties.
 */
export declare function exact<T extends object>(state: SchemaState<T>, value?: boolean): void;
/**
 * Require field to be an object with every property being a schema type.
 * Will rebuild the object (if not a class instance) and type cast values.
 * @internal
 */
export declare function of<T extends object>(state: SchemaState<T>, schemas: Blueprint<T>): Criteria<T>;
//# sourceMappingURL=shapes.d.ts.map